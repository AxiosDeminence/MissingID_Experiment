{
  "name": "missing_id_crawler",
  "description": "",
  "main": "src/module.js",
  "scripts": {
    "build": "npm install cheerio && make lib/missing_id.so && node-gyp configure && node-gyp build",
    "dev": "node --experimental-fetch ./src/module.js --base-dir ./sample-data/"
  },
  "gypfile": true,
  "dependencies": {
    "cheerio": "^1.0.0-rc.10",
    "minimist": "^1.2.6"
  }
}
